# A quick little python script.  It exists only so that I can test out the
# OpenShift-specific stuff (build, deployment, etc...) without actually
# running the oddmon aggregator.
import time
while True:
    print("Hello OpenShift")
    time.sleep(5)


